package com.stephanHogenboom.util;

public class MasterClasserHelper {

    public boolean validateEmail(String email) {
        //TODO verify that the email has the following format: atleast 5 character, then a @, then at least 3
        //TODO alpha characters, then a dot and then at least two alpha characters
        return true;
    }

    public boolean validatePhoneNumber(String phoneNumber) {
        //TODO validate that the string is numeric and has 10 numeric characters
        //TODO for bonus points. If the string contains a ´-´ (stripe) then the string must contain 11 characters.
        //TODO the - should either be on position 3 (mobile number) or position 4 (house phone number)
        return true;
    }

}
